{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run server",
            "command": [
                "python3 -m http.server 8080"
            ],
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Open in Firefox",
            "linux": {
                "command": "firefox localhost:8080 --start-debugger-server",
            },
            "isBackground": true,
            "type": "shell"
        }
    ]
}
